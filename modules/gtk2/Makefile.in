# Makefile for the CLISP module set gtk2

srcdir = @srcdir@
CC = @CC@
CPPFLAGS = @CPPFLAGS@
CFLAGS = @CFLAGS@
LIBS = @LIBS@
# defined by AM_PATH_GTK_2_0
GTK_CFLAGS = @GTK_CFLAGS@
GTK_LIBS = @GTK_LIBS@
# defined by PKG_CHECK_MODULES
LIBGLADE_CFLAGS = @LIBGLADE_CFLAGS@
LIBGLADE_LIBS = @LIBGLADE_LIBS@

CLISP = clisp -q -norc
INCLUDES = $$($(CLISP) -b)/linkkit

LN = ln
LN_S = ln -s

MAKE = make

SHELL = /bin/sh

DISTRIBFILES = gtk.o link.sh Makefile \
	$(srcdir)/gtk-server.cfg $(srcdir)/gtk.lisp $(srcdir)/ui.glade \
	$(srcdir)/preload.lisp
distribdir =

all : gtk.fas gtk.o preload.lisp

gtk.fas gtk.c : $(srcdir)/gtk.lisp $(srcdir)/gtk-server.cfg
	$(CLISP) -c $< -o ./

gtk.o : gtk.c config.h
	$(CC) $(CPPFLAGS) $(CFLAGS) -I$(INCLUDES) $(GTK_CFLAGS) \
		$(LIBGLADE_CFLAGS) -c gtk.c

preload.lisp : $(srcdir)/preload.lisp
	$(LN_S) $< .

# Make a module
clisp-module : all

# Make a module distribution into $(distribdir)
clisp-module-distrib : clisp-module force
	$(LN) $(DISTRIBFILES) $(distribdir)

clean : force
	rm -f core *.o *.a

distclean : clean

force :
