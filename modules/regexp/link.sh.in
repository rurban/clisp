file_list='regexi.o'
mod_list='regexp'
for extra in @ALLOCA@ @REGEX_O@; do
  file_list="${file_list} ../gllib/${extra}";
done
make clisp-module \
     CC="${CC}" CPPFLAGS="${CPPFLAGS}" CFLAGS="${CFLAGS}" \
     INCLUDES="$absolute_linkkitdir"
NEW_FILES="$file_list regexp.dvi"
NEW_LIBS="$file_list"
NEW_MODULES="$mod_list"
TO_LOAD='regexp'
TO_PRELOAD='preload.lisp'
