# Makefile for CLISP module set zlib

srcdir = @srcdir@
CC = @CC@
CPPFLAGS = @CPPFLAGS@
CFLAGS = @CFLAGS@
CLISP = clisp -q -norc
INCLUDES = $$($(CLISP) -b)/linkkit

LN = ln

MAKE = make

SHELL = /bin/sh

DISTRIBFILES = zlib.o link.sh Makefile $(srcdir)/zlib.lisp
distribdir =

all : zlib.o zlib.fas

zlib.c zlib.fas : $(srcdir)/zlib.lisp
	$(CLISP) -c $< -o ./

zlib.o : zlib.c config.h
	$(CC) $(CPPFLAGS) $(CFLAGS) -I$(INCLUDES) -c zlib.c

# Make a module
clisp-module : all

# Make a module distribution into $(distribdir)
clisp-module-distrib : clisp-module force
	$(LN) $(DISTRIBFILES) $(distribdir)

clean : force
	rm -f core *.o *.a

distclean : clean

force :

