file_list='wildcard.o'
for extra in '' @FNMATCH_O@; do
  test -n "${extra}" && file_list="${file_list} ../gllib/${extra}";
done
make clisp-module \
     CC="${CC}" CPPFLAGS="${CPPFLAGS}" CFLAGS="${CFLAGS}" \
     INCLUDES="$absolute_linkkitdir"
NEW_FILES="$file_list wildcard.dvi"
NEW_LIBS="$file_list"
NEW_MODULES="wildcard"
TO_LOAD='wildcard'
