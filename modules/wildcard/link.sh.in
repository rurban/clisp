file_list='wildcard.o'
mod_list='wildcard'
if test -z "@FNMATCH_O@"; then # use included gllib fnmatch implementation
  file_list="$file_list ../gllib/@FNMATCH_O@";
fi
make clisp-module \
     CC="${CC}" CPPFLAGS="${CPPFLAGS}" CFLAGS="${CFLAGS}" \
     INCLUDES="$absolute_linkkitdir"
NEW_FILES="$file_list wildcard.dvi"
NEW_LIBS="$file_list"
NEW_MODULES="$mod_list"
TO_LOAD='wildcard'
